<%= form_for(@station, :html => {:data => {:ajax => 'false'}}) do |f| %>
  <% if @station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@station.errors.count, "error") %> prohibited this station from being saved:</h2>

      <ul>
      <% @station.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <ul data-role="listview" data-inset="true">
    <li data-role="fieldcontain">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </li>
    <li data-role="fieldcontain">
      <%= f.label :description %>
      <%= f.text_area :description %>
    </li>
<!--     <li data-role="fieldcontain">
      <%= f.label 'twitter page' %>
      <%= f.text_field :station_url, :"placeholder"=>"Optional" %>
    </li> -->
    <li class="ui-body ui-body-b">
      <fieldset class="ui-grid-a">
        <div class="ui-block-a">
          <%= f.submit %>
        </div>
        <div class="ui-block-b">        
          <%= link_to 'Cancel', @station, :"data-theme"=>"a", :"data-role"=>'button' %>
        </div>
    </li> 
  </ul>
<!--   <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label 'twitter page' %><br />
    <%= f.text_field :station_url, :"placeholder"=>"Optional" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
