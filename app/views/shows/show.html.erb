<div data-role="header" data-theme="a">
    <h1><%= @show.name %></h1>
</div>
<div data-role="content">
	<p id="notice"><%= notice %></p>
	<p>
		<b>Show Name:</b>
		<%= @show.name %>
	</p>
	<p>
		<b>Show Host:</b>
		<%= @show.djname %>
	</p>	
	<p>
		<b>Show Category:</b>
		<%= @show.category %>
	</p>
	<p>
		<b>Show description:</b>
		<%= @show.description %>
	</p>
	<%= link_to 'Edit', edit_show_path(@show), :"data-role"=>"button", :"data-inline"=>"true" %>
	<%= link_to 'Back', shows_path, :"data-role"=>"button", :"data-theme"=>"b",:"data-inline"=>"true" %>
</div>